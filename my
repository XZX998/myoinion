# 1. 先在电脑上创建一个和仓库同名的文件夹（比如 myionin）
mkdir myionin
cd myionin

# 2. 初始化本地Git仓库
git init

# 3. 关联你的GitHub远程仓库（复制你页面上的仓库地址）
git remote add origin https://github.com/XZX998/myionin.git

# 4. 把你的代码文件放进这个文件夹，然后执行下面的命令
git add .
git commit -m "第一次提交：上传我的项目文件"
git branch -M main
git push -u origin main
